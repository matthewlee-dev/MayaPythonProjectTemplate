{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Maya Python Project Template","text":"<p>A starting point for anyone writing Python tools in Maya. You get a repo that already knows how to test itself, check its own code, build docs, and produce a tool users install by dragging a file into Maya.</p> <p>No prior GitHub Actions or packaging knowledge needed; each page explains what a piece does and why you'd care.</p>"},{"location":"#start-here","title":"Start here","text":"<ol> <li>Create your project: make the repo and clone it.</li> <li>Project layout: what all these files are.</li> <li>Writing your tool: where your code goes and how to    run it in Maya.</li> </ol> <p>Then, when you need them:</p> <ul> <li>Testing: checking your tool still works.</li> <li>Automatic checks: what happens when you push code.</li> <li>Releasing: shipping a version people can install.</li> <li>Documentation: this site, for your project.</li> </ul>"},{"location":"ci/","title":"Automatic checks","text":"<p>GitHub runs a set of checks on your code every time you push, and marks the commit with a green tick or a red cross.</p>"},{"location":"ci/#what-runs","title":"What runs","text":"<p>Code style. Ruff checks your code for common mistakes and inconsistent formatting. Fast, and catches real bugs like unused imports and undefined names.</p> <p>Tests. Your tests run inside a real Maya, in a container, across the versions you've chosen. If you have no tests yet, this step notices and skips.</p>"},{"location":"ci/#seeing-the-results","title":"Seeing the results","text":"<p>Click the Actions tab in your repo. Green means everything passed. Red means something didn't, and clicking into it shows which check and where.</p>"},{"location":"ci/#running-the-same-checks-yourself","title":"Running the same checks yourself","text":"<pre><code>uv run ruff check .          # find problems\nuv run ruff format .         # fix formatting\n</code></pre> <p>Running <code>ruff format .</code> before you commit avoids most style failures.</p>"},{"location":"ci/#choosing-maya-versions","title":"Choosing Maya versions","text":"<p>Edit <code>.github/workflows/reusable-maya-tests.yml</code> and change the list:</p> <pre><code>maya_version: [\"2024\"]\n</code></pre> <p>Add the versions you support, e.g. <code>[\"2024\", \"2025\", \"2026\"]</code>. Each one runs separately, so more versions means a slower check.</p> <p>The Maya containers come from mottosso/docker-maya. You should hold a valid Maya licence.</p>"},{"location":"ci/#turning-automated-checks-off","title":"Turning automated checks off","text":"<p>The checks can be disabled in the repo's Actions settings.</p>"},{"location":"documentation/","title":"Documentation","text":"<p>Your project's docs site is built from the Markdown files in <code>docs/</code> by MkDocs, and published on GitHub Pages.</p>"},{"location":"documentation/#editing-it","title":"Editing it","text":"<p>Add or edit <code>.md</code> files in <code>docs/</code>, then list them in <code>mkdocs.yml</code>:</p> <pre><code>nav:\n  - index.md\n  - usage.md\n</code></pre> <p>Preview as you write with <code>uv run mkdocs serve</code>.</p>"},{"location":"documentation/#building-docs-from-docstrings","title":"Building docs from docstrings","text":"<p>Point a page at one of your modules and its docstrings become the documentation. <code>docs/reference.md</code> already does this, and you can see the result under Reference:</p> <pre><code># Reference\n\n::: my_cool_tool.example\n</code></pre> <p>Every function in that module is listed, with its arguments and docstring. Add a line per module you want documented.</p>"},{"location":"documentation/#publishing","title":"Publishing","text":"<p>Docs deploy automatically when you cut a release, to <code>https://your-name.github.io/my-cool-tool</code>.</p> <p>Enable it once: repo Settings \u2192 Pages \u2192 source gh-pages branch.</p>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#1-create-your-repo","title":"1. Create your repo","text":"<p>Go to the template repo and click Use this template \u2192 Create a new repository.</p> <p>Name it in lowercase with hyphens, like <code>my-cool-tool</code>. That name becomes your package name and shows up in the installer, so pick something you can live with. Add a short description too, as it gets written into your README.</p>"},{"location":"getting-started/#2-wait-for-setup-to-finish","title":"2. Wait for setup to finish","text":"<p>Give GitHub a few minutes to process your new repo. You can watch progress under the Actions tab.</p>"},{"location":"getting-started/#3-clone-and-set-up","title":"3. Clone and set up","text":"<pre><code>git clone https://github.com/YOUR-NAME/my-cool-tool.git\ncd my-cool-tool\n</code></pre> <p>Install uv if you don't have it. It manages your Python versions and packages.</p> <p>Then create the local environment:</p> <pre><code>uv sync --all-groups\n</code></pre> <p>This reads <code>pyproject.toml</code>, downloads everything the project needs, and puts it in a <code>.venv</code> folder. You only need to rerun it when dependencies change.</p>"},{"location":"getting-started/#4-check-everything-works","title":"4. Check everything works","text":"<pre><code>uv run ruff check .\n</code></pre> <p>If that passes with no complaints, you're set up correctly.</p>"},{"location":"getting-started/#next","title":"Next","text":"<p>Head to Writing your tool to add your first bit of code and run it in Maya.</p>"},{"location":"project-layout/","title":"Project layout","text":"<pre><code>my-cool-tool/\n\u251c\u2500\u2500 src/my_cool_tool/     \u2190 your code goes here\n\u251c\u2500\u2500 tests/                \u2190 your tests go here\n\u251c\u2500\u2500 docs/                 \u2190 your docs go here\n\u251c\u2500\u2500 scripts/              \u2190 builds the Maya module\n\u251c\u2500\u2500 .github/workflows/    \u2190 the automatic checks\n\u251c\u2500\u2500 pyproject.toml        \u2190 project settings and dependencies\n\u251c\u2500\u2500 release.py            \u2190 cuts a release\n\u251c\u2500\u2500 README.md\n\u2514\u2500\u2500 CONTRIBUTING.md\n</code></pre> <p>Your code folder is named after your repo, and that name is what you import: <code>import my_cool_tool</code>.</p>"},{"location":"reference/","title":"Reference","text":"<p>mayapythonprojecttemplate package.</p>"},{"location":"reference/#mayapythonprojecttemplate.example.create_locator","title":"<code>create_locator(name='')</code>","text":"<p>Create a locator with the given name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name to give the locator.</p> <code>''</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The name of the locator that was created.</p> Source code in <code>src/mayapythonprojecttemplate/example.py</code> <pre><code>def create_locator(name: str = \"\") -&gt; str:\n    \"\"\"Create a locator with the given name.\n\n    Args:\n        name (str): The name to give the locator.\n\n    Returns:\n        str: The name of the locator that was created.\n    \"\"\"\n    return cmds.spaceLocator(name=name)[0]\n</code></pre>"},{"location":"releasing/","title":"Releasing","text":"<p>A release turns your code into something a user can install.</p>"},{"location":"releasing/#building-a-release","title":"Building a release","text":"<p>On GitHub, go to Actions \u2192 Release \u2192 Run workflow, then type the version number (e.g. <code>1.2.4</code>).</p> <p>GitHub bumps the version, runs the checks, tags the code, builds the zip, publishes it on your repo's Releases page, and updates your docs site.</p> <p>The other three fields only apply if your tool has dependencies:</p> <ul> <li>Vendor runtime dependencies bundles your dependencies into the module, so   the user doesn't have to install them. Untick to leave them out.</li> <li>Maya versions and Platforms decide which combinations those   dependencies are built for.</li> </ul>"},{"location":"releasing/#from-the-command-line","title":"From the command line","text":"<p><code>release.py</code> works out the next version for you and starts the same workflow:</p> <pre><code>python3 release.py --bump patch\n</code></pre> from <code>1.2.3</code> <code>--bump patch</code> <code>1.2.4</code> <code>--bump minor</code> <code>1.3.0</code> <code>--bump major</code> <code>2.0.0</code> <p>Run it from <code>main</code> with everything committed and pushed. Add <code>--dry-run</code> to check the version and preflight without triggering the release.</p>"},{"location":"releasing/#what-users-get","title":"What users get","text":"<p>A zip on your Releases page. They extract it and drag <code>my_cool_tool_drag_and_drop_installer.py</code> into a Maya viewport, which copies the tool into Maya's modules folder. Any dependencies are bundled in.</p>"},{"location":"releasing/#testing-the-package-first","title":"Testing the package first","text":"<p>Build it locally without releasing:</p> <pre><code>python3 scripts/build_module.py --clean\n</code></pre> <p>That stages everything in <code>dist/</code>, so you can drag <code>dist/my_cool_tool_drag_and_drop_installer.py</code> into Maya to try the real install.</p>"},{"location":"testing/","title":"Testing","text":""},{"location":"testing/#writing-a-test","title":"Writing a test","text":"<p>Tests go in <code>tests/maya/</code>, where you'll find this example ready to uncomment:</p> <pre><code>def test_create_locator_expects_locator_created():\n    # Arrange\n    from my_cool_tool.example import create_locator\n    from maya import cmds\n\n    # Act\n    test_loc = create_locator(\"foo\")\n\n    # Assert\n    assert cmds.objExists(test_loc)\n</code></pre> <p>Two rules:</p> <ul> <li>The file is named <code>test_*.py</code> and the function <code>test_*</code>. That's how they get   found.</li> <li>Import <code>maya</code> inside the function, not at the top of the file. Outside Maya   that import fails and breaks the whole test run.</li> </ul>"},{"location":"testing/#running-tests-locally","title":"Running tests locally","text":"<p>Tests run under <code>mayapy</code>:</p> <pre><code># --python must match your Maya version (2024 = 3.10, 2025/2026 = 3.11)\nuv venv --managed-python --python 3.11 .venv-maya\nuv pip install --python .venv-maya --group test .\nPYTHONPATH=.venv-maya/lib/python3.11/site-packages mayapy -m pytest tests\n</code></pre> <p>This needs an alias to <code>mayapy</code>, or swap it for the full path.</p> <p>Any Maya tests you set up will also run automatically on GitHub when you push.</p>"},{"location":"writing-your-tool/","title":"Writing your tool","text":""},{"location":"writing-your-tool/#where-code-goes","title":"Where code goes","text":"<p>Everything lives in <code>src/my_cool_tool/</code>. The template ships an <code>example.py</code> to delete or build from.</p>"},{"location":"writing-your-tool/#running-your-code-in-maya","title":"Running your code in Maya","text":"<p>While developing, point Maya at your source folder. In Maya's Script Editor:</p> <pre><code>import sys\n\nsys.path.append(\"/full/path/to/my-cool-tool/src\")\n\nimport my_cool_tool.example as example\n\nexample.create_locator(\"test\")\n</code></pre> <p>You only need the <code>sys.path</code> line once per Maya session.</p> <p>Once you cut a release, users install it properly and none of this path juggling is needed.</p>"},{"location":"writing-your-tool/#adding-dependencies","title":"Adding dependencies","text":"<p>If your tool needs a third-party package:</p> <pre><code>uv add some-package\n</code></pre> <p>That records it in <code>pyproject.toml</code> and installs it. When you release, it gets bundled into the module so users don't have to install anything themselves.</p>"}]}